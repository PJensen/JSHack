import { defineComponent } from "../../../lib/ecs-js/index.js";

/**
 * PrayIntent — actor prays to their deity.
 * Well-timed prayer can soothe the deity, but spam angers.
 */
export const PrayIntent = defineComponent("PrayIntent", {});
