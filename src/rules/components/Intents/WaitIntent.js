import { defineComponent } from "../../../lib/ecs-js/index.js";

/**
 * WaitIntent — actor spends a turn doing nothing (advance time, regen, etc.).
 */
export const WaitIntent = defineComponent("WaitIntent", {});
